#include<stdio.h>
int main (){
int num, bin=0, place=1;
printf("Enter number");
scanf("%d", &num);
int num1=num;
//logic
while(num!=0){
int rem = num%8;
bin = bin + rem*place;
num = num/8;
place = place*10;
}
printf("Octal of %d is %d", num1, bin);
return 0;
}
